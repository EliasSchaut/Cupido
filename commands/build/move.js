module.exports = {
    name: 'move',
    description: 'Move members from one channel to another',
    aliases: ['transfer', 'shift', 'relocatet'],
    args: true,
    usage: '[from channel_id] [to channel_id]',
    guildOnly: true,
    dmOnly: false,
    restricted: true,
    execute(message, args) {

    },
};